import {
  linkUsers,
  unlinkUsers,
} from "../src/services/friendship.service.js";

describe("Friendship Service", () => {
  test(
    "Should prevent duplicate friendship",
    async () => {
      expect(true).toBe(true);
    }
  );

  test(
    "Should create friendship",
    async () => {
      expect(true).toBe(true);
    }
  );

  test(
    "Should remove friendship",
    async () => {
      expect(true).toBe(true);
    }
  );
});